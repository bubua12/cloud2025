spring:
  application:
    name: service-product
  cloud:
    nacos:
      server-addr: 192.168.31.127:8848
      username: nacos
      password: nacos
      config:
        import-check:
          enabled: false # 禁用nacos配置导入检查

server:
  port: 9000
