spring:
  application:
    name: seata-order
  datasource:
    url: jdbc:mysql://192.168.1.242:3306/order_db?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: bubua12
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      server-addr: 192.168.1.242:8848
      username: nacos
      password: nacos
      config:
        import-check:
          enabled: false

server:
  port: 22000
mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 开启MyBatis SQL日志

otel:
  propagators:
    - tracecontext
    - baggage